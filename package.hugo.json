{
  "name": "devlog",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "hugo server --bind 127.0.0.1 --disableFastRender",
    "build": "hugo --gc --minify",
    "check": "python3 scripts/check_site.py"
  }
}
